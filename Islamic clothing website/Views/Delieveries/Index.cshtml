@model IEnumerable<Islamic_clothing_website.Models.Delievery>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DelieveryDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DelieveryAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DelieveryDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DelieveryAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DelieveryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DelieveryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DelieveryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
